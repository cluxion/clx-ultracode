# Task: ultracode perf — parallel debate round + doctor command memoization

## Context
0.1.8 installed. A verified performance audit found two speedups. Both must be behavior-preserving (identical debate outcome / identical doctor verdicts). Do NOT bump the version. Do NOT touch `.grok-briefs/`.

## Opt 1 (LOW risk, BIG win): serial agent calls within a debate round
File: `src/cluxion_effort_ultracode/core/consensus.py` `_debate_round` (lines 113-136) and `_initial_positions` (104-111).
The N agents within ONE round are issued as SEQUENTIAL LLM subprocess calls in a plain Python for-loop. Within a single round the agents are mutually INDEPENDENT.
Fix: run the per-round agent calls concurrently with a bounded `concurrent.futures.ThreadPoolExecutor(max_workers=agents)` (threads are correct — each `.complete()` blocks in `subprocess.run`, releasing the GIL). Gather the results, then KEEP the EXISTING ordered-list assembly and per-agent `_validate_debate_update` so the semantics are byte-for-byte identical to the serial path.
CRITICAL preserve: round-to-round stays SEQUENTIAL (round k needs round k-1); the early-exit unanimity check after each round is preserved UNCHANGED; no prompt / schema / validation / convergence logic changes → ZERO quality/accuracy impact, only wall-clock. ~agents-× speedup (3× default, 5× with --agents 5).
Invariant: identical debate outcome (per-agent positions in the same order, convergence detection, final consensus) vs serial — only faster. Add a test with mock agents (deterministic outputs, varied per-agent latency) asserting parallel produces the IDENTICAL ordered result list and the same convergence/round count as serial.

## Opt 2 (LOW risk): doctor re-spawns identical commands
File: `src/cluxion_effort_ultracode/doctor/probes.py` (hermes_version:42, hermes_oneshot_flag:53, hermes_subprocess_launchable:65, hermes_z_flag_support:80) + `doctor/framework.py:_make_runner`.
The SAME external commands are re-spawned across probes: `hermes --help` runs twice (hermes_oneshot_flag + hermes_z_flag_support), `hermes --version` twice. ~0.4s wasted per doctor run.
Fix: memoize per-doctor-run — wrap `ctx.run` / `shutil.which` with a command-keyed cache (a dict on the DoctorContext keyed by the EXACT argv tuple) so identical commands within ONE `run()` execute once and reuse the `CompletedProcess`. Probes keep their independent pass/fail logic but read from the cache. Deterministic, read-only, no verdict change. The cache is per-run (fresh each doctor invocation) so it never staleness-leaks.
Invariant: EVERY probe's verdict is IDENTICAL; the doctor overall result/exit code is unchanged; just fewer subprocesses. ~0.4s/run faster.

## Done
- Both opts identical-behavior (debate outcome + doctor verdicts unchanged). `uv run pytest` GREEN, `uv run ruff check .` pass. No version bump. No `.grok-briefs/` edits. Concise diff.
